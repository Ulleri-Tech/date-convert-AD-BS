export type  DaysEachMonthBS = Readonly<{
    [key: string]: number[];
  }>;

//  export type datestring1 = `${number}${number}${number}${number}-${number}${number}-${number}${number}`;
//  export type datestring2 = `${number}${number}-${number}${number}-${number}${number}${number}${number}`;
//  type DateString = datestring1 | datestring2;
